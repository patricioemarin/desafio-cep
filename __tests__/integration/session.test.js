
// Ponto de autenticação (describle = categoria dos testes)
describe ('Authentication', () => {
    // Deve somar dois números
    it ('should sum two numbers', ()=> {
        const x = 2;
        const y = 4;
        const sum = x + y;
        expect(sum).toBe(6);
    });
});

// Ponto de autenticação (describle = categoria dos testes)
//describle ('Authentication', () => {
    // Deve receber o token JWT quando autenticado com credenciais válidas
    //it ('should receive JWT token when authenticated with valid credentials', ()=> {

    //});
//});

// Ponto de cadastro de usuário (describle = categoria dos testes)
//describle ('Register User', () => {
    // 
    //it ('', ()=> {

    //});
//});

